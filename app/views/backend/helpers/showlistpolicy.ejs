<%
    showListPolicy = (data) =>{
        let dataParse = (data == undefined) ? [{title:"", description:"", icon: ""}] : JSON.parse(data)
        let html = ""
        dataParse.forEach((item, index) =>{
            html+= `
            <div id="policy-dynamic-field-${index+1}" class="form-group policy-dynamic-field">
              <label for="field" class="font-weight-bold">Policy ${index+1}</label>
              <div class="row">
                <div class="col-sm-2">
                    <label for="title_policy_1[]" class="col-form-label text-sm-right">Title</label>
                </div>
                <div class="col-sm-8">
                    <input type="text" class="form-control" name="title_policy_1[]" value="${item.title}"/>
                </div>
              </div>
              <div class="row mt-2">
                <div class="col-sm-2">
                    <label for="description_policy_1[]" class="col-form-label text-sm-right">Description</label>
                </div>
                <div class="col-sm-8">
                    <input type="text" class="form-control" name="description_policy_1[]" value="${item.description}"/>
                </div>
              </div>
              <div class="row mt-2">
                <div class="col-sm-2">
                    <label for="icon_policy_1[]" class="col-form-label text-sm-right">Icon</label>
                </div>
                <div class="col-sm-8">
                    <input type="text" class="form-control" name="icon_policy_1[]" value="${item.icon}"/>
                </div>
              </div>
            </div>
             `
        })
        return html
  }

%>